% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/body_img_urls.R
\name{body_img_urls}
\alias{body_img_urls}
\title{Extract all body image URLs}
\usage{
body_img_urls(x, unique = FALSE, base = NULL)
}
\arguments{
\item{x}{Either a document, a node set or a single node.}

\item{unique}{if \code{TRUE}, then only unique URLs will be returned. Default: \code{FALSE}.}

\item{base}{if not \code{NULL}, all relative URLs will be converted
to absolute URLs. Default: \code{NULL}.}
}
\value{
character vector of extracted URLs; if no \code{<img>} \code{src} URLs were discovered
\code{charactrer(0)} will be returned.
}
\description{
Given a valid HTML/XML document, node set or single node, a
claracter vector of all \code{<body>} \code{<img src>} URLs will be returned.
If \code{base} is not \code{NULL} all relative URLs will be converted
to absolute URLs.
}
\examples{
x <- rvest::read_html("https://congress.gov/") \%>\%
body_img_urls(x,unique = TRUE, base = "https://congress.gov/")
}
